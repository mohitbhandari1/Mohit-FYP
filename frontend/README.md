# Smart Connects Frontend

## Setup

1. Install dependencies:
   ```bash
   cd frontend
   npm install
   ```
2. Start the development server:
   ```bash
   npm run dev
   ```
3. Open `http://localhost:3000` in your browser.

This frontend uses Next.js with TypeScript and Tailwind CSS. It includes pages for community listing, community details, and event listings.
